# Текущий статус проекта

Дата: 4 сентября 2026

Статус: Фундамент проекта, Supabase Auth и CI стабилизированы. V2 auth-страницы подключены, восстановление пароля работает через реальный Supabase Auth flow. `/search` работает на реальных данных Supabase без моков. Начат production-ready ingestion pipeline для каталога.

Готово:

- Next.js 15 + App Router, TypeScript, Tailwind CSS, ESLint + Prettier
- Framer Motion для интерфейсной анимации
- Supabase SSR: browser/server clients, cookie-based SSR
- Email/password login и register с подтверждением email
- `/auth/callback` для PKCE/OAuth и recovery
- Защита `/dashboard`, серверная проверка пользователя и logout
- `/login`, `/register`, `/forgot-password`, `/reset-password`
- `/auth/login`, `/auth/register`, `/auth/reset` сохранены как совместимые redirect-маршруты
- Восстановление пароля V2 использует реальный `resetPasswordForEmail` и `updateUser`, без имитаций и `console.log`
- V2 auth-дизайн подключён к рабочим маршрутам
- Старые `maket/LoginPage.tsx`, `maket/RegisterPage.tsx`, `maket/ResetPasswordPage.tsx` удалены
- Безопасный recovery callback без произвольных redirect URL
- Русский auth UI и основные ошибки
- `.env.example` без секретов; Service Role Key не используется в приложении
- GitHub Actions: install, typecheck, lint, format:check, production build
- `package.json` содержит единый `format`-скрипт `prettier --write .`
- Основной CI теперь форматирует проект перед проверками, затем выполняет typecheck, lint, format:check и production build
- Добавлен постоянный `.github/workflows/format.yml`: автоматический Prettier на push в `main` с сохранением исправлений отдельным commit
- CI run #82 успешно завершён после внедрения постоянной автоматизации форматирования
- `package.json` закрепляет Node.js `22.x`, чтобы Vercel не выполнял автоматический major upgrade
- Временные workflow автоматизации исправлений удалены; остались только постоянные `ci.yml` и `format.yml`
- Дизайн landing hero интегрирован в приложение
- Таблицы `products` и `product_offers` существуют в Supabase
- Для каталога включены безопасные SELECT RLS-политики: видны только активные товары и предложения активных товаров
- Есть индексы каталога, включая trigram по `products.canonical_name` и индексы связей/маркетплейсов
- `/search` переведён с демонстрационных моков на серверный поиск по Supabase
- Добавлен `src/lib/catalog/search.ts` с серверным поиском по названию, бренду, категории и описанию
- Добавлен `maket/SearchResults-v2.tsx` для отображения реальных товаров и предложений
- Если каталог пуст, UI честно показывает пустое состояние вместо фейковых товаров
- Добавлена таблица `ingestion_runs` для служебного журнала загрузок
- Для `product_offers` добавлена уникальность `marketplace + external_id` для дедупликации
- Для `products` добавлен вычисляемый `canonical_key` и уникальный индекс для дедупликации по нормализованному названию и бренду
- Развёрнута защищённая Supabase Edge Function `ingest-catalog` с JWT-проверкой, валидацией входных данных и upsert в `products`/`product_offers`
- Ingestion ограничен источниками `ozon`, `wildberries`, `yandex_market` и лимитом 500 предложений за запрос
- На момент последней проверки каталог содержит 0 товаров и 0 предложений

Безопасность:

- Service Role Key не используется в Next.js-приложении
- Service Role Key используется только внутри серверной Supabase Edge Function и не передаётся клиенту
- Токены не сохраняются вручную в localStorage или собственных cookies
- `/dashboard` защищён middleware и дополнительной серверной проверкой
- Recovery callback принимает только фиксированный внутренний переход `/reset-password`
- Ответ восстановления не раскрывает существование аккаунта
- Каталог доступен на чтение только для активных сущностей через RLS; клиентские INSERT/UPDATE/DELETE-политики не открыты
- Ingestion Edge Function требует валидный JWT и `app_metadata.ingestion_enabled=true`
- В ingestion разрешены только HTTPS URL и ограниченные диапазоны числовых полей

Окружение:

- Проект не запускается локально; целевая проверка — Vercel Preview.
- CI использует `npm ci` и Node.js 22.
- Для регистрации и восстановления в Supabase нужен Email provider и Redirect URLs Vercel Preview.
- Production-домен `wobuy.ru` пока не подключён к Vercel.
- Прямой Vercel-коннектор в текущей сессии недоступен; deployment проверяется через GitHub status. Последний подтверждённый Preview имеет статус `success`.

Текущий этап: Неделя 1–2 — каркас + Auth + лендинг + базовый реальный поиск завершены. Инфраструктура форматирования стабилизирована. Начат этап ingestion и нормализации каталога.

В работе:

- Реальные адаптеры API маркетплейсов и их авторизация/лимиты
- Нормализация названий, брендов, категорий и атрибутов из конкретных источников
- Полноценная стратегия повторных загрузок, ошибок и наблюдаемости ingestion

Не начинали:

- Полнотекстовое и семантическое ранжирование
- pgvector и embeddings
- AI-агенты анализа товаров/отзывов
- История цен
- Расширенные фильтры и AI Score в V2-выдаче

Блокер следующего шага: для реального сбора данных из Ozon/Wildberries/Яндекс Маркета потребуются соответствующие API-доступы/ключи и согласованные лимиты. Сам ingestion-контур уже подготовлен и безопасно изолирован от публичного клиента.
