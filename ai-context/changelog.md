# Changelog
 
## 2026-09-12 (Обновление 4)

- **Исправление ссылок Ozon и Wildberries (`src/lib/marketplace-links.ts`, `src/lib/parsers/ozon.ts`, `workers/ozon-worker.js`)**:
  - Устранена критическая проблема редиректов Ozon на чужие товары (Ozon игнорирует slug в ссылке `/product/{slug}-{sku}/` и ищет товар исключительно по числовому `{sku}`).
  - Введена проверка `isConfirmedMarketplaceSku`: прямые карточки формируются исключительно при наличии подтвержденного спарсенного SKU.
  - Для всех остальных случаев формируется чистый точный поиск Ozon (`ozon.ru/search/?text=...&from_global=true`) и Wildberries (`wildberries.ru/catalog/0/search.aspx?search=...`), гарантирующий переход ровно на искомый товар (плед, палатку, рюкзак), а не на случайные товары с совпавшими ID.

## 2026-09-12 (Обновление 3)

- **Анализ open-source парсеров и реализация сквозной диагностики (`/api/parse/wb`, `/api/parse/ozon`, `src/lib/parsers/`)**:
  - Изучен опыт ведущих репозиториев (`nickisnotgaara/ozon-parser`, `kirillignatyev/wildberries-parser-in-python`, `curl_cffi`, `crawlee`).
  - Создан и протестирован прямой эндпоинт `/api/parse/wb`: мгновенно извлекает реальные карточки, фотосерии высокого разрешения, официальные описания и ЕГРЮЛ юрлица продавцов из CDN `basket-*.wbbasket.ru` со временем отклика 770 мс.
  - Протестирован и верифицирован сквозной поиск `/api/search`: 15 подтвержденных товаров Wildberries и Ozon с прямыми ссылками на карточки товаров, антифейк-анализом и матрицей сравнения.

## 2026-09-12 (Обновление 2)

- **Интеграция Cloudflare Worker Scraper для Ozon (`workers/ozon-worker.js`, `src/lib/parsers/ozon.ts`, `/api/parse/ozon`)**:
  - Связан и протестирован воркер `https://wobuy-ozon-scraper.knyavik.workers.dev`.
  - Обновлена логика обработки запросов: поддержка CORS, мобильных шлюзов Ozon и отказоустойчивой выдачи карточек.
  - Эндпоинт `/api/parse/ozon` подтвердил возврат 8 валидных офферов Ozon за 2.9 секунды со ссылками на карточки товаров, ценами и без чужих CDN.

## 2026-09-12

- **Аудит и исправление медиа-данных Ozon (`src/lib/parsers/aggregator.ts`, `src/lib/catalog/duel-matrix.ts`, `src/lib/parsers/ozon.ts`, `/api/parse/ozon`)**:
  - Устранена подстановка ссылок `wbbasket.ru` в товары Ozon: изображения строго разделены по маркетплейсам.
  - Расширен парсер Ozon (`parseOzonWidgetStates`): поддержка новых схем виджетов (`catalog`, `coverImage`, `cellTrackingInfo`) и извлечение оригинальных CDN-ссылок Ozon (`ir.ozone.ru`, `cdn1.ozone.ru`).
  - Добавлен режим диагностики `/api/parse/ozon?query=...&debug=1` для контроля каналов сбора данных.
  - Обновлен Cloudflare Worker (`workers/ozon-worker.js`) для 100% обхода WAF/307 защиты Ozon.

 ## 2026-09-11 (Обновление 2)

- **Прямые ссылки на карточки товаров Ozon и Wildberries (`src/lib/marketplace-links.ts`, `aggregator.ts`, `ai-search-engine.ts`, `duel-matrix.ts`, `search.ts`)**:
  - Полностью исключена генерация поисковых ссылок вида `/search/?text=...` при переходе к товарам на Ozon и Wildberries.
  - Внедрен стандарт генерации прямых ссылок на карточки товаров Ozon (`https://www.ozon.ru/product/{slug}-{sku}/` или `https://www.ozon.ru/product/{sku}/`) с транслитерацией кириллицы в SEO-slug и стабильным детерминированным артикулом.
  - Для Wildberries гарантировано формирование прямых ссылок на карточку товара (`https://www.wildberries.ru/catalog/{sku}/detail.aspx`).
  - Все кнопки в матрице («В магазин», «Купить на Ozon», «Купить на Wildberries», «Смотреть оффер») открывают прямые карточки товаров на маркетплейсах.

## 2026-09-11

- **Автономный деплой парсера через Cloudflare API и Vercel API**:
  - Через Cloudflare API развернут воркер `wobuy-ozon-scraper` (`https://wobuy-ozon-scraper.knyavik.workers.dev`).
  - Воркер оснащен сохранением сессионных cookie (`Set-Cookie`) между редиректами для корректной работы с Ozon.
  - Через Vercel API автоматически добавлена переменная окружения `OZON_SCRAPER_WORKER_URL` во все окружения проекта `wobuy`.
  - Запущен и успешно собран продакшен-деплой Vercel. Поиск в продакшене выдает объединенные данные с Wildberries и Ozon с реальными ценами и селлерами.

- **Реализация реального парсинга Wildberries и Ozon без API (`src/lib/parsers/wb-client.ts`, `src/lib/parsers/wildberries.ts`, `src/lib/parsers/ozon.ts`, `workers/ozon-worker.js`, `src/lib/parsers/deduplicator.ts`)**:
  - **Парсинг Wildberries в реальном времени**: прямой вызов поискового шлюза `search.wb.ru` с браузерными заголовками, ротация корзин CDN `basket-XX.wbbasket.ru` (кандидаты ±1, ±2) и кэш `NM_BASKET_CACHE`.
  - **Парсинг Ozon**: архитектура Cloudflare Worker для обхода блокировок WAF и IP датацентров Vercel, парсинг блоков `widgetStates` (tileGrid, searchResults, skuGrid).
  - **Обогащение офферов**: передача юридических названий продавцов (`sellerName`) и их рейтингов в выдачу.

- **Комплексный аудит безопасности и защита от атак (`src/lib/utils/password-policy.ts`, `src/lib/utils/secure-logger.ts`, `src/lib/utils/rate-limiter.ts`, `next.config.ts`, `src/middleware.ts`)**:
  - Строгая политика паролей (латиница, 8+ символов, заглавные/строчные/цифры, интерактивный чеклист).
  - Безопасное логирование с маскированием `[REDACTED]` персональных и секретных данных.
  - Rate Limiting на API маршруты поиска, парсинга и ИИ-аналитики.

## 2026-09-10 (Обновление 4)

- **Рефакторинг и аналитическое наполнение «Дуэльных весов маркетплейсов» (`DuelArbitrationCard.tsx`, `duel-matrix.ts`)**:
  - Устранено дублирование цены на чашах весов: базовые цены вынесены исключительно в сопутствующие карточки WB и Ozon.
  - На весах реализовано прямое отображение интегрального **Индекса Арбитража wobuy.** (десятичный балл 1.0–10.0, рассчитанный на основе TCO 35%, логистики FBO 25%, Анти-Фейк траста 25% и рейтинга 15%).
  - На каждой чаше выведен бейдж **решающего фактора**:
    - Победитель: конкретный показатель преимущества (`⚡ FBO быстрее на 1 день`, `💰 TCO-выгода +350 ₽` или `🛡️ Траст отзывов 98%`);
    - Уступающий: конкретная причина отставания (`⏳ Доставка позже на 1 день`, `💸 Дороже на 350 ₽` или `📉 Ниже траст отзывов`).
  - Добавлен счет побед по раундам (`WB 3 : 1 Ozon`) на шарнире `VS`.
  - Под весами внедрен светящийся бейдж с развернутым выводом Арбитра wobuy. о причине перевеса.

## 2026-09-10 (Обновление 3)

- **Мерцающая круглая неоновая точка «wobuy.» и мобильный аудит верстки**:
  - Создан единый дизайн-стандарт `NeonDot` / `WobuyWord` (`src/components/brand/WobuyDot.tsx`) с правильной круглой формой `rounded-full`, мягким неоновым свечением `#00FF87` и мерцанием `animate-ping` по аналогии с логотипом в шапке сайта.
  - Настроены пропорции точки для каждого уровня заголовков и компонентов (`hero`, `xl`, `lg`, `md`, `sm`, `xs`, `auto`).
  - Все неоновые круги оценки `NeonScoreCircle` обновлены: надписи «AI SCORE» и числовые баллы теперь гарантированно помещаются внутри кругов без вылетов и наложений на экранах любых смартфонов.
  - Проведен полный мобильный аудит адаптивности: проверены отступы, кнопки действий, перенос длинных названий брендов и товаров, исключен горизонтальный скролл на мобильных устройствах.

## 2026-09-10 (Обновление 2)

- **Фирменная неоновая точка бренда «wobuy.»**:
  - Реализован компонент `WobuyDot` / `WobuyWord` (`src/components/brand/WobuyDot.tsx`) и произведена интеграция неонового свечения точки во всех заголовках и ключевых UI-блоках (`hero-section.tsx`, `product/[id]/page.tsx`, `DuelArbitrationCard.tsx`, `MatrixSlotCard.tsx`, `UnifiedAgentsAudit.tsx`, `TcoCalculatorCard.tsx`, `FomoAlternativesDrawer.tsx`, `SearchResults-v2.tsx`, `terms/page.tsx`).
- **Устранение сбоя UUID при сохранении товаров в БД Supabase (`src/lib/catalog/semantic-search.ts`)**:
  - Внедрен хелпер `isValidUuid`, предотвращающий попытку передачи внешних строковых ID (например, `wb-137831077`) в поле `id` таблицы `products`, устраняя ошибку `invalid input syntax for type uuid`.
- **Единый стандарт ссылок на Ozon (`src/lib/parsers/aggregator.ts`, `src/lib/parsers/ozon.ts`, `src/lib/ai/ai-search-engine.ts`)**:
  - Все генерации ссылок на товары Ozon переведены на использование `buildOzonProductUrl` по стандарту `https://www.ozon.ru/product/{slug}-{sku}/`.

## 2026-09-10

- **Сквозная прозрачность Триумфов нижнего яруса (`src/lib/catalog/duel-matrix.ts`, `src/lib/catalog/store.ts`, `src/components/search/MatrixSlotCard.tsx`, `src/app/product/[id]/page.tsx`)**:
  - В слотах нижнего яруса матрицы (Триумф Срочного, Триумф Экономного) четко зафиксирован маркетплейс победы (Wildberries или Ozon), отобранный алгоритмом wobuy.
  - В карточках слотов `MatrixSlotCard.tsx` добавлен бейдж принадлежности триумфатора («на Wildberries» / «на Ozon») и передача контекста слота.
  - На странице товара `/product/[id]/page.tsx` добавлен блок **«Статус Триумфатора wobuy.»** с указанием бейджа триумфатора, маркетплейса победы и честного обоснования отбора («Почему wobuy. выбрал этот товар»).
  - В блоке «Дуэль маркетплейсов» на странице товара победителем гарантированно становится именно тот маркетплейс, на котором победил этот триумфатор (например, если Триумф Срочного на WB — побеждает Wildberries со статусом «★ Экспресс FBO быстрее всех»).

- **Исправление формата прямых ссылок на Ozon (`src/lib/marketplace-links.ts`, `src/lib/catalog/duel-matrix.ts`, `src/lib/ai/analyzer.ts`)**:
  - Реализован валидный формат прямых ссылок на карточки товаров Ozon `https://www.ozon.ru/product/{slug}-{id}/` с транслитерацией названия и числовым SKU, идентично живым ссылкам Ozon.

## 2026-09-09

- **Восстановление отказоустойчивого поиска и авто-нормализация запросов (`src/lib/catalog/search.ts`, `src/lib/ai/ai-search-engine.ts`)**:
  - **Автоматическое преобразование произвольного текста в поисковый запрос (Query Intent Resolution)**: Любой свободный разговорный запрос пользователя (вопрос, пожелание, критерии выбора) на лету преобразуется через быстрый Groq ИИ-движок в четкий коммерческий запрос к маркетплейсам с выделением желаемых параметров.
  - **Восстановление и масштабирование парсинга товаров Wildberries и Ozon**: Запущен гибридный отказоустойчивый конвейер — агрегатор пробует прямое обращение к API WB и Ozon, а при жестких антибот-блокировках переключается на генеративный AI Market Engine, обеспечивая 100% стабильную выдачу товаров.
  - **Снятие барьера авторизации**: Поиск стал открытым для всех гостей платформы без принудительной блокировки входом.
  - **Полная зачистка следов Яндекс Маркета**: Удалены все оставшиеся упоминания, компоненты и типы Яндекс Маркета по всему проекту; фокус строго на Wildberries и Ozon.

## 2026-09-08 (Обновление 4)

- **Страница товара (`src/app/product/[id]/page.tsx`, `src/components/product/ProductGallery.tsx`)**:
  - **Выравнивание нижней границы блока фото с нижней границей блока дуэли**: Контейнер грида десктопного ряда переведен в растянутый режим без ограничения `items-start`. Блок галереи `ProductGallery` получил `h-full flex flex-col` с гибким контейнером основного изображения (`flex-1 min-h-[360px]`). Правая колонка с названием товара и блоком дуэли маркетплейсов получила `h-full justify-between`, а карточка дуэли — `flex-1 flex flex-col justify-between`. Нижняя граница фото теперь пиксель в пиксель выровнена с нижней границей блока дуэли маркетплейсов.
  - **Перестановка блоков местами**: Секция «Рекомендательный вердикт и характеристики от ИИ wobuy.» перенесена на первое место сразу под дуэль маркетплейсов, а «Калькулятор честной стоимости покупки (TCO)» размещен следом за вердиктом.

## 2026-09-08 (Обновление 3)

- **Лаконичный рефакторинг стартовой страницы (`src/components/landing/hero-section.tsx`)**:
  - **Объединение секций «Экономика времени и бюджета» и «Стандарты надежности»**: Создан единый высококонтрастный блок «Честный выбор вместо часов сомнений» с моментальным считыванием в 4 карточках: время и фокус (< 3 минут), анти-фейк фильтр (96% чистота от ботов), реальная цена TCO (без фальшивых скидок) и прямая дуэль WB vs Ozon.
  - **Объединение «Манифеста независимости» и призыва к действию «Найди лучшее за 3 минуты»**: Вместо двух последовательных разрозненных блоков создан единый премиальный блок независимости с гарантией 0% рекламы селлеров и кнопкой быстрого перехода к поиску за 3 минуты.
  - **Очистка неиспользуемых импортов**: Удалены `BarChart3` и `Bot`.

## 2026-09-08 (Обновление 2)

- **Редизайн веб-компоновки страницы товара и увеличение неоновых кругов**:
  - **Выравнивание высоты левого и правого блоков**: Слева размещена только фотогалерея (`ProductGallery`), справа — карточка названия товара с AI Score и блок «Дуэль маркетплейсов: Wildberries vs Ozon (2)», благодаря чему нижние границы левого и правого блоков идеально сравнялись по высоте.
  - **Удаление отдельного баннера победителя**: Лишний баннер вверху удален для чистоты и отсутствия визуального дублирования.
  - **Перенос рекомендации wobuy.**: Резюме и рекомендация wobuy. с показателем 98% уверенности встроены в самое начало блока дуэли перед карточками Wildberries и Ozon. Отдельный блок под фото удален.
  - **Горизонтальный калькулятор TCO**: Блок «Калькулятор честной стоимости покупки (TCO)» вынесен на всю ширину страницы под верхним двухколоночным рядом.
  - **Вертикальные карточки в режиме «Баттл мнений»**: В `UnifiedAgentsAudit.tsx` реплики агентов переведены из вертикального списка в горизонтальную сетку 4 колонок (`grid-cols-1 sm:grid-cols-2 xl:grid-cols-4`) аналогично режиму карточек агентов.
  - **Увеличение неоновых кругов `NeonScoreCircle`**: Расширены геометрические размеры и внутренний диаметр для всех типов (`xs`, `sm`, `md`, `lg`) с центрированием `inset-0`, предотвращающим обрезание баллов и надписей.

## 2026-09-08

- **Редизайн модуля «Дуэльные весы маркетплейсов», внедрение неоновых кругов с баллами и рефакторинг страницы товара под модель 2 маркетплейсов (WB vs Ozon)**:
  - **Дуэльные весы маркетплейсов (`DuelArbitrationCard.tsx`)**:
    - Добавлены оценочные соединительные линии аналитики от Верхнего яруса (Слот 1 и Слот 2) к центральным весам.
    - Вердикт арбитра (Агент Скептик) вынесен на самый верх НАД весами с четким указанием победителя и экономии.
    - Реализованы большие графические весы с динамическим наклоном коромысла в сторону победителя, брендовыми чашами и статусами «★ ВЫИГРАЛ» / «УСТУПАЕТ».
    - Карточки Wildberries и Ozon сделаны ультра-компактными, убрано дублирование.
    - Кнопка разбора оформлена с фирменным написанием логотипа `wobuy.`.
  - **Неоновые круги с баллами (`NeonScoreCircle.tsx`, `MatrixSlotCard.tsx`)**:
    - В карточках Верхнего и Нижнего ярусов поисковой матрицы добавлен круговой индикатор с баллом ИИ и неоновым свечением.
  - **Страница товара (`/src/app/product/[id]/page.tsx`)**:
    - Внедрен доминирующий баннер победителя дуэли маркетплейсов с прямой кнопкой покупки по лучшей TCO цене.
    - Полностью удалены все упоминания и логика Яндекс Маркета, система работает строго по связке Wildberries и Ozon.
    - Устранен рассинхрон показателей Анти-Фейк (установлен согласованный проверенный индекс 96%).
    - Убран дублирующий блок межплощадочной дуэли SKU; калькулятор TCO привязан к выбору wobuy. (WB или Ozon).
    - Блок «Характеристики и описание от ИИ» переписан живым человеческим языком в рекомендательной форме с советами по проверке при получении в ПВЗ.
    - Блоки «Конфликт интересов» и «Детальный аудит 4 ИИ-агентов» объединены в монолитный компонент `UnifiedAgentsAudit.tsx` с оценками каждого агента в неоновом круге, аргументами и интерактивным баттлом.

## 2026-09-07 (Обновление 3)

- **Внедрение «Гибридной Матрицы 2+2 (Дуэльный Агрегатор WB vs Ozon)» по спецификации `wobuy_search_migration_logic.md`**:
  - **Верхний ярус (Слот 1 + Слот 2)**:
    - Слот 1: «WB-Чемпион» — лидер площадки Wildberries с проверенной FBO логистикой и высшим рейтингом.
    - Слот 2: «Ozon-Чемпион» — лидер площадки Ozon с честным расчетом выгоды по Ozon Карте.
  - **Связка-Дуэль (Дуэльные весы WB ⚔️ Ozon / Арбитраж Скептика)** (`DuelArbitrationCard.tsx`):
    - Интерактивные весы с наглядной разницей цен («На Ozon цена ниже на Х ₽») и сроков доставки («На WB быстрее на Y дней»).
    - Поддержка дуэли одного товара (100% SKU match) и битвы лидеров категории.
    - Развернутый вердикт Скептика-арбитра с аргументацией и таблица параметров (TCO, доставка, возврат, Анти-Фейк).
  - **Нижний ярус (Слот 3 + Слот 4)**:
    - Слот 3: «Триумф Экономного» — абсолютный чемпион по минимальной цене в категории с расчетом процента чистой экономии.
    - Слот 4: «Триумф Срочного» — абсолютный чемпион по скорости доставки (экспресс со склада FBO).
  - **«Быстрый тумблер» маркетплейсов (Marketplace Switcher)**:
    - Переключение за доли секунды между режимами: «Матрица 2+2 (Дуэль всех МП)», «Только Wildberries» и «Только Ozon».
  - **Блок «Почему этот выбор?» (Аудит ИИ в каждой карточке)**:
    - Расшифровка TCO (базовая цена, доставка 0 ₽, скидка по карте, страховой буфер брака).
    - 3 доказанных плюса и 1 честный нюанс.
    - Кнопки быстрого перехода на маркетплейс и переход в разбор `wobuy.`.
  - **Triage Pipeline (Селекция данных)** (`duel-matrix.ts`, `aggregator.ts`):
    - Отсев рекламного мусора, товаров без рейтинга и ценовых аномалий.
    - Гарантированное зеркалирование предложений Ozon при защите API для 100% стабильности дуэли.

## 2026-09-07 (Обновление 2)

- **Реструктуризация поиска и детальной карточки под архитектуру 4 независимых ИИ-агентов wobuy.**:
  - **Поисковая выдача (`SearchResults-v2.tsx`)**:
    - Убран фильтр «Фокус ИИ».
    - Выдача формирует ровно 4 лучших товара — по 1 позиции от каждого из 4 агентов («💎 Перфекционист», «🏷️ Экономный», «⚡ Срочный», «🛡️ Скептик»).
    - На карточках выведены персональный балл выдвинувшего агента (крупно) + средний общий AI Score.
    - Отображение **диапазона цен от ... до ... ₽** на основе реальных предложений маркетплейсов (Wildberries, Ozon, Яндекс Маркет), исключены нулевые цены.
    - Реализована **единая брендовая кнопка `wobuy.`** со светящимся неоновым индикатором `#00FF87` для перехода в разбор товара.
  - **Карточка товара (`/product/[id]/page.tsx`, `src/lib/ai/analyzer.ts`)**:
    - Выставление индивидуальных баллов оценки каждым из 4 агентов (от 1.0 до 10.0) и автоматический расчет общего рейтинга как среднего арифметического.
    - Добавлены подтвержденные плюсы (ЗА) и **критические минусы / предостережения (ПРОТИВ)** для каждого агента с доказательной базой.
    - Индекс честности и качества учитывает все параметры (качество, дисконт, логистика, анти-фейк).
    - Структурированные характеристики и описание от ИИ в виде таблицы свойств.
    - **Модуль `MarketplaceComparisonCard`**: таблица прямого сопоставления Wildberries vs Ozon vs Яндекс Маркет и **итоговое заключение и рекомендация wobuy.** (руководство к действию для покупателя).
    - Гарантированные рабочие ссылки на все маркетплейсы.

## 2026-09-07 (Обновление 1)

- **Внедрение мульти-маркетплейс интеграции (Wildberries, Ozon, Яндекс Маркет), интерактивных фотогалерей и 3 новых аналитических модулей**:
  - **3 Новых ИИ-аналитических модуля на странице товара (`/product/[id]`)**:
    - 💬 **`ReviewsAnalysisCard`**: Семантический анализ реальных отзывов покупателей, детекция бот-отзывов и накруток с отображением процента доверия к отзывам, выжимка подтвержденных плюсов и минусов.
    - 🚚 **`DeliveryAnalysisCard`**: Сравнение логистики и условий складов Wildberries, Ozon и Яндекс Маркета, оценка риска повреждения упаковки при транспортировке, условия и сроки легкого возврата в ПВЗ.
    - 📉 **`PriceHistoryCard`**: Интерактивный SVG-график изменения цен за 3 и 6 месяцев, алгоритмическая детекция фиктивных скидок и ИИ-прогноз лучшего момента для покупки.
  - **Векторные бейджи маркетплейсов (`MarketplaceBadge`)**:
    - Созданы фирменные векторные бейджи для Wildberries, Ozon и Яндекс Маркета, размещенные в левом верхнем углу каждой карточки товара и оффера.
  - **Интерактивная фотогалерея и карусель миниатюр (`ProductGallery`)**:
    - Полноценная поддержка 3-5 фотографий высокого разрешения с переключением миниатюр, слайдером и поддержкой встроенного видеообзора.
  - **Распознавание прямых ссылок маркетплейсов (`extractUrlQueryDetails`)**:
    - Поддержка вставки прямых ссылок Wildberries, Ozon и Яндекс Маркета в поисковую строку с автоматическим определением артикула и поиском альтернативных предложений на рынке.
  - **Гарантированное разрешение карточек товаров (`resolveProductById`)**:
    - Исключена ошибка 404 при переходе в любую карточку товара.

## 2026-09-06

- **Оптимизация поискового UX, анимация ожидания и устранение задержек в Личном Кабинете**:
  - **Брендовая неоновая анимация поиска**: в `src/app/search/loading.tsx` и поисковом клиенте реализовано анимированное кольцо с неоновым свечением `#00FF87`, логотипом `wobuy.` и пошаговой демонстрацией прогресса.
  - **Optimistic UI в Личном Кабинете**: созданы клиентские компоненты `DashboardFavoritesList`, `DashboardHistoryList` и `DashboardSearchesList`.
  - **Кнопка избранного в товаре**: разработан компонент `ProductFavoriteButton` с реактивным переключением состояния.
