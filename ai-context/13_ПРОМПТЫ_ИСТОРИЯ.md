# История промптов

## 2026-09-06

### Оптимизация UX поиска (неоновое кольцо, статус-бар) и мгновенный UI Личного Кабинета

Задача:
1. Добавить брендовую анимацию поиска в стиле `wobuy.` (неоновое кольцо, индикация перебора офферов и проверки Анти-Фейк), чтобы сайт не казался зависшим.
2. Устранить задержки и долгие ожидания в Личном Кабинете при удалении товаров из Избранного, Истории и Сохранённых поисков (внедрить Optimistic UI).
3. Обеспечить корректность ссылок покупки («Купить» / «В магазин» -> прямой переход в маркетплейс).
4. Проверить детерминированность ID товаров для исключения 404 ошибок.

Результат:
1. Созданы и внедрены компоненты `DashboardFavoritesList`, `DashboardHistoryList`, `DashboardSearchesList` и `ProductFavoriteButton` с мгновенной реакцией интерфейса без лагов.
2. Реализована интерактивная анимация поиска с неоновым кольцом и пояснением этапов работы агрегатора `wobuy.`.
3. Добавлен централизованный хелпер `src/lib/supabase/admin.ts`.
4. Проведен `lint_applet` и `compile_applet` — сборка полностью зеленая.

### Исправление ошибок сборки на Vercel (ESLint & TypeScript Typecheck)

Задача:
1. Устранить ошибку компиляции на Vercel (`Command "bun run build" exited with 1`).
2. Удалить неиспользуемые переменные `Clock`, `ShoppingCart`, `TrendingDown`, `Truck` в `src/app/product/[id]/page.tsx` и `maket/SearchResults-v2.tsx`.
3. Устранить ошибку типизации свойства `deliveryText` (заменено на каноничное `delivery_text`).
4. Проверить `lint_applet` и `compile_applet`.

Результат:
1. Линтер успешно отрабатывает без ошибок (`eslint .` 0 errors, 0 warnings).
2. Полная оптимизированная сборка `next build` завершается со статусом `Compiled successfully`.
3. Проект готов к деплою на Vercel.

## 2026-09-05

### Реализация семантического векторного поиска (pgvector) и парсера намерений

Задача:
1. Создать инфраструктуру pgvector в Supabase (миграция, индекс HNSW, функция `match_products_by_embedding`).
2. Создать генератор векторных эмбеддингов (`src/lib/ai/embeddings.ts`).
3. Реализовать парсер намерений запросов (выделение ограничений цен, брендов, категорий и скрытых свойств).
4. Настроить умную LRU-синхронизацию товаров в базу с защитой от переполнения бесплатного лимита Supabase.
5. Интегрировать гибридный поиск в `src/lib/catalog/search.ts` и `/api/search`.

Результат:
1. Создан файл миграции `supabase/migrations/20260905_pgvector_semantic_search.sql`.
2. Реализованы модули `src/lib/ai/embeddings.ts` и `src/lib/catalog/semantic-search.ts`.
3. Запросы на естественном языке (например, «тихие наушники для перелетов до 35 тысяч») автоматически парсятся с извлечением максимальной цены (35 000 руб.) и семантических тегов.
4. Выполнена верификация и компиляция проекта.

### Проверка конфигурации стэка, ключей Supabase, Groq и активация 4 ИИ-агентов

Задача:
1. Проверить переданные переменные окружения (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `GROQ_API_KEY`).
2. Проверить прямое подключение к базе данных Supabase, наличие таблиц и готовность расширения pgvector.
3. Протестировать подключение к Groq API и определить актуальные рабочие модели (протестирована и подключена `openai/gpt-oss-120b`).
4. Подключить модуль динамического ИИ-анализа по 4 архетипам к карточке товара (`/product/[id]`).

Результат:
1. База данных Supabase и все 6 основных таблиц доступны и готовы.
2. Groq API успешно отвечает и генерирует структурированный JSON с оценками и тезисами 4 агентов («Перфекционист», «Экономный», «Срочный», «Скептик / Анти-Фейк»).
3. Реализован модуль `src/lib/ai/analyzer.ts` и эндпоинт `/api/ai/analyze` с резервным переключением на Gemini API.
4. Проект успешно собирается и проходит линтинг.

### Реализация Этапа 1: Конвейер парсинга и агрегации Wildberries и Ozon

Задача:
1. Реализовать реальный конвейер парсинга данных Wildberries и Ozon в соответствии с инструкциями из `ai-context/Парсеры.md`.
2. Интегрировать открытые JSON-эндпоинты Wildberries (`search.wb.ru`, `card.wb.ru`) и вычисление корзин картинок (`basket-01`...`basket-23`).
3. Реализовать отказоустойчивый парсер Ozon с обработкой поисковых выдач и прямыми ссылками.
4. Разработать алгоритм дедупликации, кластеризации и объединения предложений с разных площадок в единую каноническую карточку `wobuy.`.
5. Связать конвейер с поиском (`/search`), карточкой товара (`/product/[id]`) и предоставить открытые API (`/api/search`, `/api/parse/wb`, `/api/parse/ozon`).

Результат:
1. Созданы модули `src/lib/parsers/types.ts`, `src/lib/parsers/wb-utils.ts`, `src/lib/parsers/wildberries.ts`, `src/lib/parsers/ozon.ts`, `src/lib/parsers/deduplicator.ts`, `src/lib/parsers/aggregator.ts`.
2. Добавлены и протестированы API-эндпоинты поиска и прямого парсинга.
3. Поисковый слой `src/lib/catalog/search.ts` и карточка товара бесшовно работают с живыми данными и кэшем.
4. Сборка и линтер проходят чисто без ошибок.

### Стандартизация логотипа `wobuy.` и мобильный UX фокуса ИИ

Задача:
1. Исправить мобильное отображение кнопок в разделе «Выбери свой фокус ИИ-анализа»: на смартфонах кнопки не умещались и требовали неочевидной прокрутки.
2. Стандартизировать написание бренда `wobuy.` на тёмном фоне: строчные буквы, идеально круглая точка и неоновое свечение `#00FF87` во всех разделах.
3. Пересмотреть общий план развития проекта: перейти на стратегию «Search-First» (сначала надёжная агрегация и поиск товаров, затем подключение ИИ-агентов).

Результат:
1. Создан каноничный компонент `BrandLogo` (`/src/components/brand/BrandLogo.tsx`) с круглой неоновой точкой и строгим написанием `wobuy.`. Внедрён на всех страницах и модальных окнах платформы.
2. Селектор фокуса ИИ на главной странице (`src/components/landing/hero-section.tsx`) переведён на мобильную сетку 2x2: все 4 архетипа («Перфекционист», «Экономный», «Срочный», «Анти-Фейк») теперь видны целиком без скролла.
3. Обновлены документы `12_ТЕКУЩИЙ_СТАТУС.md` и `changelog.md` с фиксацией стратегии Search-First.

### Флагманский редизайн Личного Кабинета (`/dashboard`)

Задача: провести полную ревизию и редизайн личного кабинета, обеспечив стопроцентное соответствие концепции бренда `wobuy.`, устранить неконсистентные светлые кнопки и оформить карточки избранного, истории и настроек с максимальным уровнем UX/UI.

Результат:
1. Личный кабинет переведён на премиальный тёмный стиль (`#0D0F14`, панели `#13161C`/`#171A21` со стеклянным `backdrop-blur-xl`, акцент `#00FF87`):
   - Разработана стилизованная кнопка выхода `DashboardLogoutButton` взамен стандартного белого outline.
   - 4 виджета статистики («В избранном», «Сохранено поисков», «Просмотрено», «AI Ассистент PRO 2.0»).
   - Карточки сохранённых товаров снабжены превью изображений, бейджем бренда, индикатором `AI Score`, категорией и минимальной ценой на рынке с переходом в 1 клик.
   - Списки сохранённых запросов и истории просмотров оформлены с интуитивным удалением и переходом.
   - Раздел настроек `ProfileSettings` получил стилизованные поля, статус защищённости сессий и прямую ссылку на смену пароля.
   - Добавлен блок рекомендаций ИИ и фоновый спотлайт-эффект.
2. Сборка `compile_applet` и линтер отрабатывают без ошибок.

### Устранение ошибки запуска dev-сервера и расширение главной страницы

Задача: устранить ошибку сборки/запуска dev-сервера (`Failed to collect page data for /icon.svg`), завершить начатые изменения по разделам главной страницы, мобильной навигации и экранам авторизации.

Результат:
1. Ошибка сборки устранена: статический файл иконки перемещен в `/public/icon.svg` с корректной ссылкой в root `layout.tsx`. Сборка `compile_applet` проходит со статусом Success.
2. Главная страница (`src/components/landing/hero-section.tsx`) получила полноценные разделы: «Как это работает» (3 шага), «Преимущества» (4 карточки принципов wobuy.), «Тарифы» (Базовый 0 ₽ и PRO), манифест «О нас», CTA-блок перед футером.
3. Мобильная панель `MobileBottomNav` дополнена кнопкой «Главная» и адаптирована под safe-area.
4. В формы входа и регистрации добавлена кнопка возврата на главную для мобильных пользователей.

### Ревизия главной страницы, карточки товара и устранение наложений в выдаче

Задача: исправить наложение бейджа «Анти-Фейк» на рамку карточки в выдаче, обновить страницу карточки товара (`/product/[id]`) в строгом соответствии с концепцией и шаблоном `wobuy.`, а также проверить главную страницу и привести её в полное соответствие с дизайн-системой.

Результат:
1. Выдача поиска:
   - Скорректированы размеры и геометрия `AiScoreGauge` (компактный SVG-контейнер `0 0 76 76`).
   - Исключено наложение меток на правую рамку карточки за счёт защитных классов `truncate`, `min-w-0` и адаптивных паддингов.
2. Карточка товара (`/product/[id]`):
   - Полный редизайн с круговым светящимся `AI Score`, бейджами вердикта ИИ («Анти-Фейк: 96%», «Честная скидка», «Выбор AI 2026»).
   - Детальный разбор 4 агентов («Перфекционист», «Экономный», «Срочный», «Анти-Фейк») с прозрачными аргументами.
   - Сравнение предложений продавцов, кнопки быстрой покупки на маркетплейсах, спарклайн цен, характеристики и мобильная панель навигации.
3. Главная страница:
   - Внедрён 4-й архетип `🛡️ Анти-Фейк` в интерактивном блоке.
   - Превью-карточка оснащена круговым индикатором `AI SCORE` со свечением `#00FF87`.
   - Добавлены быстрые кликабельные теги популярных запросов под строкой поиска.
   - Интегрирован `MobileBottomNav` с безопасным отступом для мобильных пользователей.

### UX/UI Overhaul выдачи поиска и Мобильная адаптация

Задача: приблизить страницу результатов поиска (`/search`) к дизайн-шаблону `wobuy_search_results_ui_mockup.png` в строгом соответствии с концепцией бренда `wobuy.` (тёмный неоновый стиль `#0D0F14`, круговой AI Score, бейджи Анти-Фейк/Честная цена, спарклайны цен, блок «АНАЛИТИКА РЫНКА») и реализовать удобную мобильную адаптацию с панелью навигации под большой палец.

Результат:
1. Полностью обновлён `maket/SearchResults-v2.tsx`:
   - Внедрён круговой неоновый SVG-индикатор `AI Score` со свечением drop-shadow `#00FF87`.
   - В каждой карточке отображаются 4 ключевых преимущества с галочками (Анти-Фейк, Честная цена, Выбор AI, маркер категории).
   - Встроен спарклайн «Динамика цен» с текущей ценой и отметками месяцев.
   - Добавлен интерактивный блок «АНАЛИТИКА РЫНКА» внизу страницы с графиком тренда цен, средней цены и лучшего окна покупки, а также бейджами изменений (+50.0%, -9.2%, +10.5%).
   - Карточки оснащены кнопками «Купить сейчас 🛒» (прямой переход к лучшему предложению маркетплейса) и «Подробнее».
2. Мобильная адаптация:
   - Создан компонент `src/components/ui/MobileBottomNav.tsx` (Поиск, Избранное, Аналитика, Профиль) с фиксацией внизу экрана на мобильных устройствах.
   - Компонент интегрирован на страницах `/search`, `/product/[id]` и `/dashboard` с безопасным отступом `pb-24`.
3. Каталог обогащён товарами из мокапа: Sennheiser Momentum 4 Wireless и Bowers & Wilkins Px8.
4. Проведены проверки: `npm run lint`, `npm run typecheck` и `npm run build` завершаются чисто без ошибок.

### UX ревизия главной, auth и каталога

Задача: устранить визуальные белые поля, лишнее пространство в auth, повторяющиеся проблемы выдачи и улучшить UX каталога.

Результат: auth-layout очищен от дублирующей панели; верхний воздух над логотипом auth уменьшен; добавлен favicon; metadata получили нормальный title/description; V2 выдача очищена от старого макета и лишних импортов; фильтры собраны в компактную раскрывающуюся панель; добавлены два режима desktop-каталога — 3 карточки в ряд и список; выбор вида сохраняется через URL; hero уплотнён, фоновые glow-эффекты не расширяют страницу; документ получил единый тёмный фон и защиту от горизонтального overflow.

### Навигация по якорям

Задача: при переходе по «Как это работает», «Тарифы» и «О нас» fixed header не должен закрывать верхнюю часть целевой секции.

Результат: нативный переход по hash заменён на программный `scrollTo` с фиксированным offset 80px; мобильная навигация использует тот же механизм и автоматически закрывается после выбора секции.

### CI контроль

Предыдущее красное сообщение про `createClient` относится к старому состоянию. Проверенный run `33916390080` завершился успешно: TypeScript, ESLint, форматирование и production build прошли. После последних изменений требуется новый CI-прогон и он является обязательной точкой контроля.

## 2026-09-04

### Создание базового каркаса проекта

Задача: создать базовый каркас на Next.js 15 с App Router, TypeScript, Tailwind CSS, ESLint и Prettier.

Результат: каркас создан и подготовлен к дальнейшей разработке.

### Подключение Supabase Auth

Задача: безопасно реализовать email/password login/register, confirmation, callback, защиту dashboard, logout и восстановление пароля.

Результат: auth flow реализован через `@supabase/ssr`, middleware и cookie-based SSR.

### Перевод auth UI на V2

Задача: использовать утверждённые V2-макеты.

Результат: auth-маршруты подключены к V2, recovery использует реальный Supabase flow.

### Реальный каталог и поиск

Задача: подключить `/search` к Supabase вместо демонстрационных моков.

Результат: серверный `searchProducts()` и V2-выдача на базе `products` + `product_offers`.

### Переход на полную демо-эмуляцию

Задача: не ждать API маркетплейсов, а отладить продукт на контролируемых демонстрационных данных.

Результат: Supabase-каталог наполнен 12 демонстрационными товарами и 16 предложениями.

### Механическая ревизия
 
Задача: устранить мёртвые действия, повторяющиеся CI-ошибки, проблемы навигации и нестабильные пользовательские сценарии.

Результат: регистрация V2 переведена на реальный Supabase Auth; добавлены loading/error states, 404, рабочие избранное/история/настройки; главная и поиск получили рабочую механику.

### Завершение ревизии и стабилизация CI
 
Задача: провести полную ревизию по маршруту auth → dashboard → карточка товара → поиск → accessibility → финальная проверка CI.
 
Результат: проверены все серверные действия и клиентские переходы, ссылки на маркетплейсы защищены `rel="noreferrer"`, интерактивные элементы снабжены доступными метками, кодовая база отформатирована с Prettier (создан `.prettierignore`), полный цикл `npm run typecheck`, `npm run lint`, `npm run format:check`, `npm run build` завершается со статусом 0.
