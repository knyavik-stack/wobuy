name: Одноразовое форматирование WOBuy

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Получение кода
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Форматирование проблемных файлов
        run: npx --yes prettier@3.6.2 --write ai-context/00_README_ДЛЯ_ИИ.md ai-context/01_SYSTEM_PROMPT.md src/components/ui/button.tsx

      - name: Сохранение форматирования
        run: |
          if git diff --quiet; then
            echo "Изменений форматирования нет."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "style: исправить оставшиеся файлы Prettier"
          git push origin HEAD:main
